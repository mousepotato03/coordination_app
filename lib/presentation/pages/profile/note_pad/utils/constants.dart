class NotepadConstants {
  static const double pageWidth = 350.0;
  static const double pageHeight = 500.0;
  static const double headerHeight = 30.0;
  static const double dragThreshold = 300.0;
  static const Duration animationDuration = Duration(milliseconds: 500);
}